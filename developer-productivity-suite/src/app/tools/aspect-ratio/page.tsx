import { AspectRatioCalc } from "@/components/tools/aspect-ratio";

export const metadata = {
    title: "Aspect Ratio Calculator | DevTools",
    description: "Calculate dimensions, resize images, and find aspect ratios.",
};

export default function AspectRatioPage() {
    return <AspectRatioCalc />;
}
