import { ListToArray } from "@/components/tools/list-to-array";

export const metadata = {
    title: "List to Array Converter | DevTools",
    description: "Convert a list of text items into a JSON array, JavaScript array, or SQL list.",
};

export default function ListToArrayPage() {
    return <ListToArray />;
}
